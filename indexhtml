<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="Art of Occasions — Premium event decoration, children's parties, birthday celebrations, chocolate arrangements, personalized candle gifts, and custom printed glass gifts.">
    <meta name="keywords" content="event decoration, party planning, children parties, birthday celebrations, chocolate arrangements, candle gifts, glass gifts, luxury events">
    <title>Art of Occasions — Turning Every Occasion Into a Beautiful Memory</title>
    <link rel="icon" href="https://i.imgur.com/WQHSNRf.jpeg" type="image/jpeg">
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:ital,wght@0,400;0,500;0,600;0,700;0,800;1,400;1,500;1,600&family=Inter:wght@300;400;500;600;700&family=Cormorant+Garamond:ital,wght@0,300;0,400;0,500;0,600;1,300;1,400;1,500&display=swap" rel="stylesheet">
    <style>
        *, *::before, *::after { margin: 0; padding: 0; box-sizing: border-box; }

        :root {
            --white: #FFFFFF;
            --black: #000000;
            --gray-50: #FAFAFA;
            --gray-100: #F5F5F5;
            --gray-200: #E5E5E5;
            --gray-300: #D4D4D4;
            --gray-400: #A3A3A3;
            --gray-500: #737373;
            --gray-600: #525252;
            --gray-700: #404040;
            --gray-800: #262626;
            --gray-900: #171717;
            --font-display: 'Playfair Display', serif;
            --font-body: 'Inter', sans-serif;
            --font-elegant: 'Cormorant Garamond', serif;
        }

        html {
            scroll-behavior: smooth;
            overflow-x: hidden;
            scrollbar-width: thin;
            scrollbar-color: var(--gray-400) transparent;
        }

        body {
            font-family: var(--font-body);
            color: var(--gray-800);
            background: var(--white);
            overflow-x: hidden;
            -webkit-font-smoothing: antialiased;
            -moz-osx-font-smoothing: grayscale;
        }

        ::-webkit-scrollbar { width: 6px; }
        ::-webkit-scrollbar-track { background: transparent; }
        ::-webkit-scrollbar-thumb { background: var(--gray-400); border-radius: 3px; }

        /* ============ PRELOADER ============ */
        .preloader {
            position: fixed;
            inset: 0;
            z-index: 99999;
            background: var(--black);
            display: flex;
            align-items: center;
            justify-content: center;
            flex-direction: column;
            transition: opacity 0.8s ease, visibility 0.8s ease;
        }
        .preloader.hidden {
            opacity: 0;
            visibility: hidden;
            pointer-events: none;
        }
        .preloader-logo {
            width: auto;
            height: 100px;
            max-width: 220px;
            border-radius: 12px;
            object-fit: contain;
            animation: preloaderPulse 1.5s ease-in-out infinite;
        }
        .preloader-bar {
            width: 120px;
            height: 2px;
            background: var(--gray-800);
            margin-top: 30px;
            border-radius: 2px;
            overflow: hidden;
        }
        .preloader-bar::after {
            content: '';
            display: block;
            width: 0%;
            height: 100%;
            background: var(--white);
            animation: preloaderFill 2s ease forwards;
        }
        @keyframes preloaderPulse {
            0%, 100% { transform: scale(1); opacity: 0.7; }
            50% { transform: scale(1.08); opacity: 1; }
        }
        @keyframes preloaderFill {
            to { width: 100%; }
        }

        /* ============ CURSOR ============ */
        .custom-cursor {
            width: 20px;
            height: 20px;
            border: 1.5px solid var(--gray-600);
            border-radius: 50%;
            position: fixed;
            pointer-events: none;
            z-index: 99998;
            transition: transform 0.15s ease, background 0.3s ease, border-color 0.3s ease;
            mix-blend-mode: difference;
            transform: translate(-50%, -50%);
        }
        .custom-cursor.hover {
            transform: translate(-50%, -50%) scale(2.2);
            background: rgba(255,255,255,0.15);
            border-color: var(--white);
        }
        @media (max-width: 768px) {
            .custom-cursor { display: none; }
        }

        /* ============ NAVBAR ============ */
        .navbar {
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            z-index: 1000;
            padding: 20px 40px;
            display: flex;
            align-items: center;
            justify-content: space-between;
            transition: all 0.5s cubic-bezier(0.4, 0, 0.2, 1);
        }
        .navbar.scrolled {
            background: rgba(255,255,255,0.85);
            backdrop-filter: blur(20px) saturate(180%);
            -webkit-backdrop-filter: blur(20px) saturate(180%);
            padding: 12px 40px;
            box-shadow: 0 1px 30px rgba(0,0,0,0.06);
        }
        .nav-logo {
            display: flex;
            align-items: center;
            gap: 14px;
            text-decoration: none;
            color: var(--white);
            transition: color 0.5s;
        }
        .navbar.scrolled .nav-logo { color: var(--black); }
        .nav-logo img {
            width: auto;
            height: 44px;
            max-width: 140px;
            border-radius: 8px;
            object-fit: contain;
            transition: transform 0.4s ease;
        }
        .nav-logo:hover img { transform: scale(1.08); }
        .nav-logo-text {
            font-family: var(--font-display);
            font-size: 1.2rem;
            font-weight: 600;
            letter-spacing: 0.5px;
        }
        .nav-links {
            display: flex;
            align-items: center;
            gap: 36px;
            list-style: none;
        }
        .nav-links a {
            text-decoration: none;
            color: var(--white);
            font-size: 0.82rem;
            font-weight: 500;
            letter-spacing: 1.5px;
            text-transform: uppercase;
            position: relative;
            padding: 4px 0;
            transition: color 0.5s;
        }
        .navbar.scrolled .nav-links a { color: var(--black); }
        .nav-links a::after {
            content: '';
            position: absolute;
            bottom: 0;
            left: 0;
            width: 0;
            height: 1px;
            background: currentColor;
            transition: width 0.4s cubic-bezier(0.4, 0, 0.2, 1);
        }
        .nav-links a:hover::after { width: 100%; }
        .nav-cta {
            padding: 10px 28px !important;
            border: 1px solid rgba(255,255,255,0.5) !important;
            border-radius: 30px;
            transition: all 0.4s ease !important;
        }
        .nav-cta::after { display: none !important; }
        .nav-cta:hover {
            background: var(--white) !important;
            color: var(--black) !important;
        }
        .navbar.scrolled .nav-cta {
            border-color: var(--black) !important;
        }
        .navbar.scrolled .nav-cta:hover {
            background: var(--black) !important;
            color: var(--white) !important;
        }

        /* Mobile Menu */
        .menu-toggle {
            display: none;
            flex-direction: column;
            gap: 5px;
            cursor: pointer;
            z-index: 1002;
            padding: 8px;
        }
        .menu-toggle span {
            width: 26px;
            height: 2px;
            background: var(--white);
            transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
            border-radius: 2px;
        }
        .navbar.scrolled .menu-toggle span { background: var(--black); }
        .menu-toggle.active span:nth-child(1) {
            transform: rotate(45deg) translate(5px, 5px);
            background: var(--white) !important;
        }
        .menu-toggle.active span:nth-child(2) { opacity: 0; }
        .menu-toggle.active span:nth-child(3) {
            transform: rotate(-45deg) translate(5px, -5px);
            background: var(--white) !important;
        }
        .mobile-menu {
            position: fixed;
            inset: 0;
            z-index: 1001;
            background: rgba(0,0,0,0.97);
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            gap: 32px;
            transform: translateX(100%);
            transition: transform 0.6s cubic-bezier(0.4, 0, 0.2, 1);
        }
        .mobile-menu.open { transform: translateX(0); }
        .mobile-menu a {
            text-decoration: none;
            color: var(--white);
            font-family: var(--font-display);
            font-size: 2rem;
            font-weight: 400;
            letter-spacing: 2px;
            opacity: 0;
            transform: translateY(30px);
            transition: all 0.4s ease;
        }
        .mobile-menu.open a {
            opacity: 1;
            transform: translateY(0);
        }
        .mobile-menu.open a:nth-child(1) { transition-delay: 0.1s; }
        .mobile-menu.open a:nth-child(2) { transition-delay: 0.2s; }
        .mobile-menu.open a:nth-child(3) { transition-delay: 0.3s; }
        .mobile-menu.open a:nth-child(4) { transition-delay: 0.4s; }
        .mobile-menu.open a:nth-child(5) { transition-delay: 0.5s; }
        .mobile-menu.open a:nth-child(6) { transition-delay: 0.6s; }

        @media (max-width: 968px) {
            .nav-links { display: none; }
            .menu-toggle { display: flex; }
            .navbar { padding: 16px 24px; }
            .navbar.scrolled { padding: 12px 24px; }
        }

        /* ============ HERO ============ */
        .hero {
            position: relative;
            height: 100vh;
            min-height: 700px;
            display: flex;
            align-items: center;
            justify-content: center;
            overflow: hidden;
            background: var(--black);
        }
        .hero-bg {
            position: absolute;
            inset: 0;
            z-index: 0;
        }
        .hero-bg-img {
            position: absolute;
            inset: 0;
            width: 100%;
            height: 100%;
            object-fit: cover;
            opacity: 0.25;
            filter: grayscale(30%);
            animation: heroZoom 25s ease-in-out infinite alternate;
        }
        @keyframes heroZoom {
            0% { transform: scale(1); }
            100% { transform: scale(1.12); }
        }
        .hero-overlay {
            position: absolute;
            inset: 0;
            background: linear-gradient(180deg,
                rgba(0,0,0,0.5) 0%,
                rgba(0,0,0,0.3) 40%,
                rgba(0,0,0,0.6) 100%
            );
            z-index: 1;
        }

        /* Floating shapes */
        .floating-shapes {
            position: absolute;
            inset: 0;
            z-index: 1;
            pointer-events: none;
            overflow: hidden;
        }
        .float-shape {
            position: absolute;
            border: 1px solid rgba(255,255,255,0.07);
            border-radius: 50%;
            animation: floatShape 20s ease-in-out infinite;
        }
        .float-shape:nth-child(1) { width: 300px; height: 300px; top: 10%; left: -5%; animation-delay: 0s; animation-duration: 22s; }
        .float-shape:nth-child(2) { width: 200px; height: 200px; top: 60%; right: -3%; animation-delay: -5s; animation-duration: 18s; }
        .float-shape:nth-child(3) { width: 150px; height: 150px; bottom: 10%; left: 20%; animation-delay: -10s; animation-duration: 25s; }
        .float-shape:nth-child(4) { width: 100px; height: 100px; top: 20%; right: 15%; animation-delay: -7s; animation-duration: 20s; border-radius: 0; transform: rotate(45deg); }
        .float-shape:nth-child(5) { width: 250px; height: 250px; top: 40%; left: 50%; animation-delay: -3s; animation-duration: 30s; }
        .float-shape:nth-child(6) { width: 60px; height: 60px; top: 15%; left: 40%; animation-delay: -12s; animation-duration: 15s; background: rgba(255,255,255,0.02); }
        @keyframes floatShape {
            0%, 100% { transform: translateY(0) rotate(0deg); }
            33% { transform: translateY(-30px) rotate(5deg); }
            66% { transform: translateY(20px) rotate(-3deg); }
        }

        .hero-content {
            position: relative;
            z-index: 2;
            text-align: center;
            padding: 0 24px;
            max-width: 900px;
        }
        .hero-logo {
            width: auto;
            height: 140px;
            max-width: 300px;
            border-radius: 14px;
            object-fit: contain;
            margin: 0 auto 36px;
            display: block;
            border: 2px solid rgba(255,255,255,0.12);
            box-shadow: 0 0 60px rgba(255,255,255,0.08);
            animation: heroLogoPulse 4s ease-in-out infinite;
            background: rgba(255,255,255,0.05);
            padding: 6px;
        }
        @keyframes heroLogoPulse {
            0%, 100% { box-shadow: 0 0 60px rgba(255,255,255,0.08); }
            50% { box-shadow: 0 0 80px rgba(255,255,255,0.15); }
        }
        .hero-badge {
            display: inline-block;
            font-size: 0.7rem;
            letter-spacing: 4px;
            text-transform: uppercase;
            color: rgba(255,255,255,0.6);
            margin-bottom: 28px;
            font-weight: 500;
            border: 1px solid rgba(255,255,255,0.15);
            padding: 8px 24px;
            border-radius: 30px;
            backdrop-filter: blur(10px);
        }
        .hero-title {
            font-family: var(--font-display);
            font-size: clamp(2.2rem, 5.5vw, 4.2rem);
            font-weight: 400;
            color: var(--white);
            line-height: 1.2;
            margin-bottom: 28px;
            letter-spacing: -0.5px;
        }
        .hero-title em {
            font-style: italic;
            font-family: var(--font-elegant);
            font-weight: 300;
        }
        .hero-sub {
            font-family: var(--font-elegant);
            font-size: clamp(1rem, 2vw, 1.35rem);
            color: rgba(255,255,255,0.55);
            font-weight: 300;
            max-width: 580px;
            margin: 0 auto 44px;
            line-height: 1.7;
            letter-spacing: 0.5px;
        }
        .hero-buttons {
            display: flex;
            gap: 18px;
            justify-content: center;
            flex-wrap: wrap;
        }
        .btn-primary {
            display: inline-flex;
            align-items: center;
            gap: 10px;
            padding: 16px 40px;
            background: var(--white);
            color: var(--black);
            text-decoration: none;
            font-size: 0.8rem;
            font-weight: 600;
            letter-spacing: 2px;
            text-transform: uppercase;
            border-radius: 50px;
            transition: all 0.5s cubic-bezier(0.4, 0, 0.2, 1);
            border: none;
            cursor: pointer;
            position: relative;
            overflow: hidden;
        }
        .btn-primary::before {
            content: '';
            position: absolute;
            inset: 0;
            background: var(--black);
            transform: scaleX(0);
            transform-origin: right;
            transition: transform 0.5s cubic-bezier(0.4, 0, 0.2, 1);
            border-radius: 50px;
        }
        .btn-primary:hover::before { transform: scaleX(1); transform-origin: left; }
        .btn-primary span { position: relative; z-index: 1; }
        .btn-primary:hover { color: var(--white); box-shadow: 0 20px 50px rgba(0,0,0,0.3); }
        .btn-secondary {
            display: inline-flex;
            align-items: center;
            gap: 10px;
            padding: 16px 40px;
            background: transparent;
            color: var(--white);
            text-decoration: none;
            font-size: 0.8rem;
            font-weight: 500;
            letter-spacing: 2px;
            text-transform: uppercase;
            border-radius: 50px;
            border: 1px solid rgba(255,255,255,0.3);
            transition: all 0.5s cubic-bezier(0.4, 0, 0.2, 1);
            cursor: pointer;
        }
        .btn-secondary:hover {
            background: rgba(255,255,255,0.1);
            border-color: rgba(255,255,255,0.6);
        }
        .scroll-indicator {
            position: absolute;
            bottom: 40px;
            left: 50%;
            transform: translateX(-50%);
            z-index: 2;
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 10px;
            color: rgba(255,255,255,0.4);
            animation: scrollBounce 2s ease-in-out infinite;
        }
        .scroll-indicator span {
            font-size: 0.6rem;
            letter-spacing: 3px;
            text-transform: uppercase;
        }
        .scroll-line {
            width: 1px;
            height: 40px;
            background: linear-gradient(to bottom, rgba(255,255,255,0.4), transparent);
            animation: scrollLineAnim 2s ease-in-out infinite;
        }
        @keyframes scrollBounce {
            0%, 100% { transform: translateX(-50%) translateY(0); }
            50% { transform: translateX(-50%) translateY(8px); }
        }
        @keyframes scrollLineAnim {
            0% { opacity: 1; }
            50% { opacity: 0.3; }
            100% { opacity: 1; }
        }

        /* ============ SECTION COMMONS ============ */
        .section { padding: 120px 24px; position: relative; }
        .section-dark { background: var(--black); color: var(--white); }
        .section-light { background: var(--white); color: var(--black); }
        .section-gray { background: var(--gray-50); color: var(--black); }
        .container { max-width: 1280px; margin: 0 auto; }
        .section-header {
            text-align: center;
            margin-bottom: 80px;
        }
        .section-label {
            font-size: 0.7rem;
            letter-spacing: 4px;
            text-transform: uppercase;
            color: var(--gray-400);
            margin-bottom: 20px;
            display: block;
            font-weight: 500;
        }
        .section-dark .section-label { color: var(--gray-500); }
        .section-title {
            font-family: var(--font-display);
            font-size: clamp(2rem, 4vw, 3.2rem);
            font-weight: 400;
            line-height: 1.2;
            margin-bottom: 20px;
        }
        .section-title em {
            font-style: italic;
            font-family: var(--font-elegant);
            font-weight: 300;
        }
        .section-desc {
            font-family: var(--font-elegant);
            font-size: 1.15rem;
            color: var(--gray-500);
            max-width: 560px;
            margin: 0 auto;
            line-height: 1.7;
            font-weight: 300;
        }
        .section-dark .section-desc { color: var(--gray-400); }
        .divider-line {
            width: 50px;
            height: 1px;
            background: var(--gray-300);
            margin: 24px auto 0;
        }
        .section-dark .divider-line { background: var(--gray-700); }

        /* ============ ABOUT / INTRO ============ */
        .intro-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 80px;
            align-items: center;
        }
        .intro-image-wrap {
            position: relative;
        }
        .intro-image-wrap img {
            width: 100%;
            height: 550px;
            object-fit: cover;
            border-radius: 8px;
            filter: grayscale(15%);
            transition: all 0.8s cubic-bezier(0.4, 0, 0.2, 1);
        }
        .intro-image-wrap:hover img {
            filter: grayscale(0%);
            transform: scale(1.02);
        }
        .intro-image-frame {
            position: absolute;
            top: 20px;
            left: 20px;
            right: -20px;
            bottom: -20px;
            border: 1px solid var(--gray-200);
            border-radius: 8px;
            z-index: -1;
            transition: all 0.6s ease;
        }
        .intro-image-wrap:hover .intro-image-frame {
            top: 15px;
            left: 15px;
            right: -15px;
            bottom: -15px;
        }
        .intro-text-content h3 {
            font-family: var(--font-display);
            font-size: clamp(1.8rem, 3vw, 2.5rem);
            font-weight: 400;
            margin-bottom: 24px;
            line-height: 1.3;
        }
        .intro-text-content h3 em {
            font-family: var(--font-elegant);
            font-style: italic;
            font-weight: 300;
        }
        .intro-text-content p {
            font-size: 1rem;
            line-height: 1.9;
            color: var(--gray-600);
            margin-bottom: 18px;
        }
        .intro-stats {
            display: flex;
            gap: 50px;
            margin-top: 40px;
            padding-top: 40px;
            border-top: 1px solid var(--gray-200);
        }
        .intro-stat-num {
            font-family: var(--font-display);
            font-size: 2.5rem;
            font-weight: 600;
            color: var(--black);
            line-height: 1;
        }
        .intro-stat-label {
            font-size: 0.75rem;
            color: var(--gray-500);
            letter-spacing: 1px;
            text-transform: uppercase;
            margin-top: 6px;
        }
        @media (max-width: 868px) {
            .intro-grid { grid-template-columns: 1fr; gap: 50px; }
            .intro-image-wrap img { height: 400px; }
        }

        /* ============ SERVICES ============ */
        .services-grid {
            display: grid;
            gap: 100px;
        }
        .service-item {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 70px;
            align-items: center;
        }
        .service-item:nth-child(even) .service-image-col { order: 2; }
        .service-item:nth-child(even) .service-text-col { order: 1; }

        .service-image-col {
            position: relative;
            overflow: hidden;
            border-radius: 10px;
        }
        .service-img {
            width: 100%;
            height: 480px;
            object-fit: cover;
            border-radius: 10px;
            filter: grayscale(10%);
            transition: all 0.8s cubic-bezier(0.4, 0, 0.2, 1);
        }
        .service-item:nth-child(1) .service-img { height: 520px; }
        .service-item:nth-child(2) .service-img { height: 450px; }
        .service-item:nth-child(3) .service-img { height: 500px; }
        .service-item:nth-child(4) .service-img { height: 440px; }
        .service-item:nth-child(5) .service-img { height: 490px; }

        .service-image-col:hover .service-img {
            transform: scale(1.05);
            filter: grayscale(0%);
        }
        .service-image-overlay {
            position: absolute;
            inset: 0;
            background: linear-gradient(180deg, transparent 50%, rgba(0,0,0,0.2) 100%);
            border-radius: 10px;
            pointer-events: none;
        }
        .service-number {
            font-family: var(--font-display);
            font-size: 5rem;
            font-weight: 700;
            color: var(--gray-100);
            line-height: 1;
            margin-bottom: 16px;
        }
        .service-text-col h3 {
            font-family: var(--font-display);
            font-size: clamp(1.6rem, 2.8vw, 2.2rem);
            font-weight: 500;
            margin-bottom: 20px;
            line-height: 1.3;
        }
        .service-text-col p {
            font-size: 1rem;
            line-height: 1.9;
            color: var(--gray-600);
            margin-bottom: 32px;
        }
        .service-features {
            list-style: none;
            margin-bottom: 36px;
        }
        .service-features li {
            font-size: 0.9rem;
            color: var(--gray-600);
            padding: 8px 0;
            border-bottom: 1px solid var(--gray-100);
            display: flex;
            align-items: center;
            gap: 10px;
        }
        .service-features li::before {
            content: '✦';
            font-size: 0.6rem;
            color: var(--gray-400);
        }
        .btn-book {
            display: inline-flex;
            align-items: center;
            gap: 12px;
            padding: 14px 36px;
            background: var(--black);
            color: var(--white);
            text-decoration: none;
            font-size: 0.78rem;
            font-weight: 600;
            letter-spacing: 2px;
            text-transform: uppercase;
            border-radius: 50px;
            transition: all 0.5s cubic-bezier(0.4, 0, 0.2, 1);
            position: relative;
            overflow: hidden;
        }
        .btn-book::before {
            content: '';
            position: absolute;
            inset: 0;
            background: var(--gray-800);
            transform: scaleX(0);
            transform-origin: right;
            transition: transform 0.5s cubic-bezier(0.4, 0, 0.2, 1);
            border-radius: 50px;
        }
        .btn-book:hover::before { transform: scaleX(1); transform-origin: left; }
        .btn-book span, .btn-book svg { position: relative; z-index: 1; }
        .btn-book:hover { box-shadow: 0 15px 40px rgba(0,0,0,0.2); transform: translateY(-2px); }
        .btn-book svg {
            width: 16px;
            height: 16px;
            transition: transform 0.4s ease;
        }
        .btn-book:hover svg { transform: translateX(4px); }

        @media (max-width: 868px) {
            .service-item {
                grid-template-columns: 1fr;
                gap: 36px;
            }
            .service-item:nth-child(even) .service-image-col { order: 0; }
            .service-item:nth-child(even) .service-text-col { order: 0; }
            .service-img,
            .service-item:nth-child(1) .service-img,
            .service-item:nth-child(2) .service-img,
            .service-item:nth-child(3) .service-img,
            .service-item:nth-child(4) .service-img,
            .service-item:nth-child(5) .service-img {
                height: 320px;
            }
            .service-number { font-size: 3rem; }
        }

        /* ============ GALLERY ============ */
        .gallery-masonry {
            columns: 3;
            column-gap: 20px;
        }
        .gallery-item {
            break-inside: avoid;
            margin-bottom: 20px;
            position: relative;
            overflow: hidden;
            border-radius: 8px;
            cursor: pointer;
        }
        .gallery-item img {
            width: 100%;
            display: block;
            border-radius: 8px;
            filter: grayscale(15%);
            transition: all 0.7s cubic-bezier(0.4, 0, 0.2, 1);
        }
        .gallery-item:hover img {
            transform: scale(1.06);
            filter: grayscale(0%);
        }
        .gallery-overlay {
            position: absolute;
            inset: 0;
            background: linear-gradient(180deg, transparent 40%, rgba(0,0,0,0.5) 100%);
            opacity: 0;
            transition: opacity 0.5s ease;
            border-radius: 8px;
            display: flex;
            align-items: flex-end;
            padding: 24px;
        }
        .gallery-item:hover .gallery-overlay { opacity: 1; }
        .gallery-overlay span {
            color: var(--white);
            font-family: var(--font-elegant);
            font-size: 1.1rem;
            font-weight: 400;
            letter-spacing: 1px;
        }
        @media (max-width: 768px) {
            .gallery-masonry { columns: 2; column-gap: 12px; }
            .gallery-item { margin-bottom: 12px; }
        }
        @media (max-width: 480px) {
            .gallery-masonry { columns: 1; }
        }

        /* ============ WHY CHOOSE US ============ */
        .why-grid {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 30px;
        }
        .why-card {
            padding: 48px 36px;
            background: rgba(255,255,255,0.03);
            border: 1px solid rgba(255,255,255,0.06);
            border-radius: 16px;
            backdrop-filter: blur(10px);
            transition: all 0.6s cubic-bezier(0.4, 0, 0.2, 1);
            position: relative;
            overflow: hidden;
        }
        .why-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 1px;
            background: linear-gradient(90deg, transparent, rgba(255,255,255,0.15), transparent);
            opacity: 0;
            transition: opacity 0.5s ease;
        }
        .why-card:hover::before { opacity: 1; }
        .why-card:hover {
            background: rgba(255,255,255,0.06);
            border-color: rgba(255,255,255,0.12);
            transform: translateY(-8px);
        }
        .why-icon {
            width: 52px;
            height: 52px;
            border: 1px solid rgba(255,255,255,0.1);
            border-radius: 14px;
            display: flex;
            align-items: center;
            justify-content: center;
            margin-bottom: 28px;
            font-size: 1.3rem;
            transition: all 0.5s ease;
        }
        .why-card:hover .why-icon {
            background: rgba(255,255,255,0.06);
            transform: scale(1.1);
        }
        .why-card h4 {
            font-family: var(--font-display);
            font-size: 1.25rem;
            font-weight: 500;
            color: var(--white);
            margin-bottom: 14px;
        }
        .why-card p {
            font-size: 0.92rem;
            color: var(--gray-500);
            line-height: 1.7;
        }
        @media (max-width: 968px) {
            .why-grid { grid-template-columns: repeat(2, 1fr); }
        }
        @media (max-width: 580px) {
            .why-grid { grid-template-columns: 1fr; }
            .why-card { padding: 36px 28px; }
        }

        /* ============ TESTIMONIALS ============ */
        .testimonials-track {
            display: flex;
            gap: 30px;
            overflow: hidden;
        }
        .testimonial-card {
            min-width: calc(33.333% - 20px);
            padding: 48px 36px;
            background: var(--white);
            border: 1px solid var(--gray-100);
            border-radius: 16px;
            transition: all 0.5s ease;
            position: relative;
        }
        .testimonial-card:hover {
            box-shadow: 0 30px 60px rgba(0,0,0,0.06);
            transform: translateY(-6px);
        }
        .testimonial-quote {
            font-family: var(--font-display);
            font-size: 3rem;
            color: var(--gray-200);
            line-height: 1;
            margin-bottom: 16px;
        }
        .testimonial-text {
            font-family: var(--font-elegant);
            font-size: 1.1rem;
            color: var(--gray-600);
            line-height: 1.8;
            margin-bottom: 28px;
            font-style: italic;
        }
        .testimonial-author {
            display: flex;
            align-items: center;
            gap: 14px;
        }
        .testimonial-avatar {
            width: 44px;
            height: 44px;
            border-radius: 50%;
            background: var(--gray-100);
            display: flex;
            align-items: center;
            justify-content: center;
            font-family: var(--font-display);
            font-size: 1rem;
            font-weight: 600;
            color: var(--gray-500);
        }
        .testimonial-name {
            font-weight: 600;
            font-size: 0.92rem;
            color: var(--black);
        }
        .testimonial-role {
            font-size: 0.78rem;
            color: var(--gray-400);
            margin-top: 2px;
        }
        .testimonial-stars {
            color: var(--gray-800);
            font-size: 0.72rem;
            letter-spacing: 3px;
            margin-bottom: 20px;
        }
        @media (max-width: 968px) {
            .testimonial-card { min-width: calc(50% - 15px); }
        }
        @media (max-width: 600px) {
            .testimonial-card { min-width: 100%; }
        }

        /* ============ CTA SECTION ============ */
        .cta-section {
            padding: 140px 24px;
            text-align: center;
            position: relative;
            overflow: hidden;
            background: var(--black);
        }
        .cta-bg-text {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            font-family: var(--font-display);
            font-size: clamp(5rem, 15vw, 14rem);
            font-weight: 700;
            color: rgba(255,255,255,0.02);
            white-space: nowrap;
            pointer-events: none;
            letter-spacing: -5px;
        }
        .cta-content {
            position: relative;
            z-index: 1;
        }
        .cta-title {
            font-family: var(--font-display);
            font-size: clamp(2rem, 4.5vw, 3.5rem);
            font-weight: 400;
            color: var(--white);
            margin-bottom: 22px;
            line-height: 1.2;
        }
        .cta-title em {
            font-family: var(--font-elegant);
            font-style: italic;
            font-weight: 300;
        }
        .cta-desc {
            font-family: var(--font-elegant);
            font-size: 1.15rem;
            color: var(--gray-500);
            margin-bottom: 44px;
            max-width: 500px;
            margin-left: auto;
            margin-right: auto;
            line-height: 1.7;
        }
        .btn-cta {
            display: inline-flex;
            align-items: center;
            gap: 12px;
            padding: 18px 50px;
            background: var(--white);
            color: var(--black);
            text-decoration: none;
            font-size: 0.8rem;
            font-weight: 600;
            letter-spacing: 2px;
            text-transform: uppercase;
            border-radius: 50px;
            transition: all 0.5s cubic-bezier(0.4, 0, 0.2, 1);
            position: relative;
            overflow: hidden;
        }
        .btn-cta::before {
            content: '';
            position: absolute;
            inset: 0;
            background: var(--gray-200);
            transform: scaleX(0);
            transform-origin: right;
            transition: transform 0.5s cubic-bezier(0.4, 0, 0.2, 1);
            border-radius: 50px;
        }
        .btn-cta:hover::before { transform: scaleX(1); transform-origin: left; }
        .btn-cta span, .btn-cta svg { position: relative; z-index: 1; }
        .btn-cta:hover { box-shadow: 0 20px 60px rgba(255,255,255,0.1); transform: translateY(-2px); }

        /* ============ FOOTER ============ */
        .footer {
            background: var(--black);
            color: var(--white);
            padding: 80px 24px 0;
            border-top: 1px solid rgba(255,255,255,0.06);
        }
        .footer-grid {
            max-width: 1280px;
            margin: 0 auto;
            display: grid;
            grid-template-columns: 2fr 1fr 1fr 1fr;
            gap: 60px;
            padding-bottom: 60px;
            border-bottom: 1px solid rgba(255,255,255,0.06);
        }
        .footer-brand-logo {
            display: flex;
            align-items: center;
            gap: 14px;
            margin-bottom: 24px;
        }
        .footer-brand-logo img {
            width: auto;
            height: 52px;
            max-width: 160px;
            border-radius: 8px;
            object-fit: contain;
        }
        .footer-brand-logo span {
            font-family: var(--font-display);
            font-size: 1.3rem;
            font-weight: 600;
        }
        .footer-brand-desc {
            font-size: 0.92rem;
            color: var(--gray-500);
            line-height: 1.8;
            max-width: 320px;
        }
        .footer-col h5 {
            font-family: var(--font-display);
            font-size: 0.95rem;
            font-weight: 600;
            margin-bottom: 24px;
            color: var(--white);
        }
        .footer-col ul {
            list-style: none;
        }
        .footer-col ul li {
            margin-bottom: 14px;
        }
        .footer-col ul li a {
            text-decoration: none;
            color: var(--gray-500);
            font-size: 0.88rem;
            transition: color 0.3s ease;
        }
        .footer-col ul li a:hover { color: var(--white); }
        .footer-contact-item {
            display: flex;
            align-items: center;
            gap: 10px;
            margin-bottom: 14px;
            color: var(--gray-500);
            font-size: 0.88rem;
        }
        .footer-contact-item svg {
            width: 16px;
            height: 16px;
            flex-shrink: 0;
        }
        .footer-bottom {
            max-width: 1280px;
            margin: 0 auto;
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 28px 0;
            font-size: 0.78rem;
            color: var(--gray-600);
        }
        @media (max-width: 868px) {
            .footer-grid { grid-template-columns: 1fr 1fr; gap: 40px; }
        }
        @media (max-width: 580px) {
            .footer-grid { grid-template-columns: 1fr; gap: 36px; }
            .footer-bottom { flex-direction: column; gap: 12px; text-align: center; }
        }

        /* ============ SCROLL ANIMATIONS ============ */
        .reveal {
            opacity: 0;
            transform: translateY(50px);
            transition: all 0.9s cubic-bezier(0.4, 0, 0.2, 1);
        }
        .reveal.active {
            opacity: 1;
            transform: translateY(0);
        }
        .reveal-left {
            opacity: 0;
            transform: translateX(-60px);
            transition: all 0.9s cubic-bezier(0.4, 0, 0.2, 1);
        }
        .reveal-left.active {
            opacity: 1;
            transform: translateX(0);
        }
        .reveal-right {
            opacity: 0;
            transform: translateX(60px);
            transition: all 0.9s cubic-bezier(0.4, 0, 0.2, 1);
        }
        .reveal-right.active {
            opacity: 1;
            transform: translateX(0);
        }
        .reveal-scale {
            opacity: 0;
            transform: scale(0.9);
            transition: all 0.9s cubic-bezier(0.4, 0, 0.2, 1);
        }
        .reveal-scale.active {
            opacity: 1;
            transform: scale(1);
        }

        /* ============ LIGHTBOX ============ */
        .lightbox {
            position: fixed;
            inset: 0;
            z-index: 10000;
            background: rgba(0,0,0,0.95);
            display: flex;
            align-items: center;
            justify-content: center;
            opacity: 0;
            visibility: hidden;
            transition: all 0.4s ease;
            cursor: pointer;
        }
        .lightbox.open {
            opacity: 1;
            visibility: visible;
        }
        .lightbox img {
            max-width: 90%;
            max-height: 90vh;
            object-fit: contain;
            border-radius: 6px;
            transform: scale(0.9);
            transition: transform 0.4s ease;
        }
        .lightbox.open img { transform: scale(1); }
        .lightbox-close {
            position: absolute;
            top: 24px;
            right: 32px;
            color: var(--white);
            font-size: 2rem;
            cursor: pointer;
            background: none;
            border: none;
            font-family: var(--font-body);
            font-weight: 300;
            transition: transform 0.3s ease;
        }
        .lightbox-close:hover { transform: rotate(90deg); }

        /* ============ PARALLAX ============ */
        .parallax-section {
            position: relative;
            height: 50vh;
            min-height: 350px;
            overflow: hidden;
            display: flex;
            align-items: center;
            justify-content: center;
        }
        .parallax-bg {
            position: absolute;
            inset: -100px;
            background-size: cover;
            background-position: center;
            filter: grayscale(30%);
        }
        .parallax-overlay {
            position: absolute;
            inset: 0;
            background: rgba(0,0,0,0.6);
        }
        .parallax-content {
            position: relative;
            z-index: 1;
            text-align: center;
            color: var(--white);
        }
        .parallax-content h2 {
            font-family: var(--font-display);
            font-size: clamp(1.8rem, 4vw, 3rem);
            font-weight: 400;
            margin-bottom: 10px;
        }
        .parallax-content h2 em {
            font-family: var(--font-elegant);
            font-style: italic;
            font-weight: 300;
        }
        .parallax-content p {
            font-family: var(--font-elegant);
            font-size: 1.1rem;
            color: rgba(255,255,255,0.6);
        }

        /* ============ MISC ============ */
        .section { position: relative; }
        .section-bg-pattern {
            position: absolute;
            inset: 0;
            pointer-events: none;
            overflow: hidden;
        }
        .section-bg-pattern::after {
            content: '';
            position: absolute;
            top: -50%;
            right: -10%;
            width: 500px;
            height: 500px;
            border: 1px solid rgba(0,0,0,0.03);
            border-radius: 50%;
        }

        /* Back to top */
        .back-to-top {
            position: fixed;
            bottom: 30px;
            right: 30px;
            width: 48px;
            height: 48px;
            background: var(--black);
            color: var(--white);
            border: none;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            cursor: pointer;
            z-index: 999;
            opacity: 0;
            transform: translateY(20px);
            transition: all 0.4s ease;
            box-shadow: 0 4px 20px rgba(0,0,0,0.15);
        }
        .back-to-top.visible {
            opacity: 1;
            transform: translateY(0);
        }
        .back-to-top:hover {
            transform: translateY(-4px);
            box-shadow: 0 8px 30px rgba(0,0,0,0.25);
        }
        .back-to-top svg { width: 18px; height: 18px; }

        /* ============ RESPONSIVE FINE TUNE ============ */
        @media (max-width: 768px) {
            .section { padding: 80px 20px; }
            .section-header { margin-bottom: 50px; }
            .services-grid { gap: 70px; }
            .hero-logo { height: 110px; max-width: 240px; }
            .hero-badge { font-size: 0.6rem; letter-spacing: 3px; }
        }
        @media (max-width: 480px) {
            .section { padding: 60px 16px; }
            .hero-buttons { flex-direction: column; align-items: center; }
            .intro-stats { gap: 30px; }
            .intro-stat-num { font-size: 2rem; }
        }
    </style>
</head>
<body>

    <!-- Preloader -->
    <div class="preloader" id="preloader">
        <img src="https://i.imgur.com/WQHSNRf.jpeg" alt="Art of Occasions" class="preloader-logo">
        <div class="preloader-bar"></div>
    </div>

    <!-- Custom Cursor -->
    <div class="custom-cursor" id="cursor"></div>

    <!-- Navbar -->
    <nav class="navbar" id="navbar">
        <a href="#hero" class="nav-logo">
            <img src="https://i.imgur.com/WQHSNRf.jpeg" alt="Art of Occasions Logo">
            <span class="nav-logo-text">Art of Occasions</span>
        </a>
        <ul class="nav-links">
            <li><a href="#about">About</a></li>
            <li><a href="#services">Services</a></li>
            <li><a href="#gallery">Gallery</a></li>
            <li><a href="#testimonials">Reviews</a></li>
            <li><a href="https://wa.me/96181139230" target="_blank" class="nav-cta">Book Now</a></li>
        </ul>
        <div class="menu-toggle" id="menuToggle">
            <span></span>
            <span></span>
            <span></span>
        </div>
    </nav>

    <!-- Mobile Menu -->
    <div class="mobile-menu" id="mobileMenu">
        <a href="#about">About</a>
        <a href="#services">Services</a>
        <a href="#gallery">Gallery</a>
        <a href="#why">Why Us</a>
        <a href="#testimonials">Reviews</a>
        <a href="https://wa.me/96181139230" target="_blank">Book Now</a>
    </div>

    <!-- Hero Section -->
    <section class="hero" id="hero">
        <div class="hero-bg">
            <img src="https://i.imgur.com/uQgyKtv.jpeg" alt="Luxury Event Decoration" class="hero-bg-img">
            <div class="hero-overlay"></div>
        </div>
        <div class="floating-shapes">
            <div class="float-shape"></div>
            <div class="float-shape"></div>
            <div class="float-shape"></div>
            <div class="float-shape"></div>
            <div class="float-shape"></div>
            <div class="float-shape"></div>
        </div>
        <div class="hero-content">
            <img src="https://i.imgur.com/WQHSNRf.jpeg" alt="Art of Occasions" class="hero-logo">
            <span class="hero-badge">Premium Event Design</span>
            <h1 class="hero-title">Turning Every Occasion<br>Into a <em>Beautiful Memory</em></h1>
            <p class="hero-sub">We craft extraordinary celebrations with exquisite attention to detail, transforming your most cherished moments into timeless works of art.</p>
            <div class="hero-buttons">
                <a href="#services" class="btn-primary"><span>Explore Services</span></a>
                <a href="https://wa.me/96181139230" target="_blank" class="btn-secondary">Book a Consultation</a>
            </div>
        </div>
        <div class="scroll-indicator">
            <span>Scroll</span>
            <div class="scroll-line"></div>
        </div>
    </section>

    <!-- About / Intro Section -->
    <section class="section section-light" id="about">
        <div class="container">
            <div class="intro-grid">
                <div class="intro-image-wrap reveal-left">
                    <img src="https://i.imgur.com/VusrOeO.jpeg" alt="Art of Occasions Event Setup">
                    <div class="intro-image-frame"></div>
                </div>
                <div class="intro-text-content reveal-right">
                    <span class="section-label">Our Story</span>
                    <h3>Where Creativity Meets <em>Elegance</em></h3>
                    <p>At Art of Occasions, we believe every celebration deserves to be extraordinary. We blend artistic vision with meticulous craftsmanship to create unforgettable experiences that reflect the uniqueness of each client.</p>
                    <p>From intimate birthday gatherings to grand celebrations, our team transforms spaces into breathtaking environments that captivate every guest and leave lasting impressions.</p>
                    <div class="intro-stats">
                        <div>
                            <div class="intro-stat-num">500+</div>
                            <div class="intro-stat-label">Events Styled</div>
                        </div>
                        <div>
                            <div class="intro-stat-num">100%</div>
                            <div class="intro-stat-label">Client Satisfaction</div>
                        </div>
                        <div>
                            <div class="intro-stat-num">5+</div>
                            <div class="intro-stat-label">Years of Excellence</div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Parallax Divider -->
    <div class="parallax-section" data-parallax>
        <div class="parallax-bg" style="background-image: url('https://i.imgur.com/ljOl2Bs.jpeg');" data-parallax-bg></div>
        <div class="parallax-overlay"></div>
        <div class="parallax-content reveal">
            <h2>Crafted with <em>Passion</em></h2>
            <p>Every detail designed to perfection</p>
        </div>
    </div>

    <!-- Services Section -->
    <section class="section section-light" id="services">
        <div class="container">
            <div class="section-header reveal">
                <span class="section-label">What We Offer</span>
                <h2 class="section-title">Our Premium <em>Services</em></h2>
                <p class="section-desc">Each service is thoughtfully designed to exceed expectations, combining creativity with luxury to make your occasion truly remarkable.</p>
                <div class="divider-line"></div>
            </div>

            <div class="services-grid">
                <!-- Service 1 -->
                <div class="service-item">
                    <div class="service-image-col reveal-left">
                        <img src="https://i.imgur.com/uQgyKtv.jpeg" alt="Children's Party Decorations" class="service-img" loading="lazy">
                        <div class="service-image-overlay"></div>
                    </div>
                    <div class="service-text-col reveal-right">
                        <span class="service-number">01</span>
                        <h3>Children's Parties</h3>
                        <p>Create a magical world for your little ones with our enchanting party decorations. From whimsical themes for girls to adventurous setups for boys, we design every detail to spark joy and wonder in young hearts.</p>
                        <ul class="service-features">
                            <li>Custom themed decorations</li>
                            <li>Balloon artistry & installations</li>
                            <li>Character-inspired setups</li>
                            <li>Interactive decor elements</li>
                        </ul>
                        <a href="https://wa.me/96181139230" target="_blank" class="btn-book">
                            <span>Book Your Service</span>
                            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M5 12h14M12 5l7 7-7 7"/></svg>
                        </a>
                    </div>
                </div>

                <!-- Service 2 -->
                <div class="service-item">
                    <div class="service-image-col reveal-right">
                        <img src="https://i.imgur.com/VusrOeO.jpeg" alt="Birthday Celebrations" class="service-img" loading="lazy">
                        <div class="service-image-overlay"></div>
                    </div>
                    <div class="service-text-col reveal-left">
                        <span class="service-number">02</span>
                        <h3>Birthday Celebrations</h3>
                        <p>Elevate your birthday into an unforgettable affair. Our bespoke birthday preparations encompass everything from sophisticated table settings to dramatic centerpieces, ensuring every moment is picture-perfect.</p>
                        <ul class="service-features">
                            <li>Elegant table arrangements</li>
                            <li>Custom backdrop designs</li>
                            <li>Luxury floral compositions</li>
                            <li>Ambient lighting design</li>
                        </ul>
                        <a href="https://wa.me/96181139230" target="_blank" class="btn-book">
                            <span>Book Your Service</span>
                            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M5 12h14M12 5l7 7-7 7"/></svg>
                        </a>
                    </div>
                </div>

                <!-- Service 3 -->
                <div class="service-item">
                    <div class="service-image-col reveal-left">
                        <img src="https://i.imgur.com/ljOl2Bs.jpeg" alt="Chocolate Arrangements" class="service-img" loading="lazy">
                        <div class="service-image-overlay"></div>
                    </div>
                    <div class="service-text-col reveal-right">
                        <span class="service-number">03</span>
                        <h3>Chocolate Arrangements</h3>
                        <p>Indulge in the art of luxury chocolate presentation. Our exquisite chocolate arrangements are crafted for special occasions, blending premium confections with artistic display to create a centerpiece that delights all senses.</p>
                        <ul class="service-features">
                            <li>Premium chocolate selection</li>
                            <li>Artistic presentation design</li>
                            <li>Custom packaging & wrapping</li>
                            <li>Perfect for gifting occasions</li>
                        </ul>
                        <a href="https://wa.me/96181139230" target="_blank" class="btn-book">
                            <span>Book Your Service</span>
                            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M5 12h14M12 5l7 7-7 7"/></svg>
                        </a>
                    </div>
                </div>

                <!-- Service 4 -->
                <div class="service-item">
                    <div class="service-image-col reveal-right">
                        <img src="https://i.imgur.com/z9w8jhi.jpeg" alt="Personalized Candle Gifts" class="service-img" loading="lazy">
                        <div class="service-image-overlay"></div>
                    </div>
                    <div class="service-text-col reveal-left">
                        <span class="service-number">04</span>
                        <h3>Personalized Candle Gifts</h3>
                        <p>Illuminate special moments with our bespoke candle arrangements, accompanied by personalized cards that speak from the heart. Each piece is a thoughtfully curated expression of warmth and elegance.</p>
                        <ul class="service-features">
                            <li>Hand-curated candle collections</li>
                            <li>Custom calligraphy cards</li>
                            <li>Luxury gift packaging</li>
                            <li>Scented & decorative options</li>
                        </ul>
                        <a href="https://wa.me/96181139230" target="_blank" class="btn-book">
                            <span>Book Your Service</span>
                            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M5 12h14M12 5l7 7-7 7"/></svg>
                        </a>
                    </div>
                </div>

                <!-- Service 5 -->
                <div class="service-item">
                    <div class="service-image-col reveal-left">
                        <img src="https://i.imgur.com/zsyNbEJ.jpeg" alt="Custom Printed Glass Gifts" class="service-img" loading="lazy">
                        <div class="service-image-overlay"></div>
                    </div>
                    <div class="service-text-col reveal-right">
                        <span class="service-number">05</span>
                        <h3>Custom Printed Glass Gifts</h3>
                        <p>Transform ordinary glass into extraordinary keepsakes with our precision printing service. From personalized mugs to elegant glassware, each piece carries your unique message, making it a cherished memento for any occasion.</p>
                        <ul class="service-features">
                            <li>High-quality glass printing</li>
                            <li>Personalized text & images</li>
                            <li>Variety of glass products</li>
                            <li>Ideal for corporate & personal gifts</li>
                        </ul>
                        <a href="https://wa.me/96181139230" target="_blank" class="btn-book">
                            <span>Book Your Service</span>
                            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M5 12h14M12 5l7 7-7 7"/></svg>
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Gallery Section -->
    <section class="section section-gray" id="gallery">
        <div class="container">
            <div class="section-header reveal">
                <span class="section-label">Portfolio</span>
                <h2 class="section-title">Our <em>Gallery</em></h2>
                <p class="section-desc">A curated collection of our finest creations, each one a testament to our commitment to excellence.</p>
                <div class="divider-line"></div>
            </div>
            <div class="gallery-masonry">
                <div class="gallery-item reveal-scale" onclick="openLightbox(this)">
                    <img src="https://i.imgur.com/uQgyKtv.jpeg" alt="Gallery 1" loading="lazy">
                    <div class="gallery-overlay"><span>Children's Party</span></div>
                </div>
                <div class="gallery-item reveal-scale" onclick="openLightbox(this)">
                    <img src="https://i.imgur.com/z9w8jhi.jpeg" alt="Gallery 2" loading="lazy">
                    <div class="gallery-overlay"><span>Candle Arrangements</span></div>
                </div>
                <div class="gallery-item reveal-scale" onclick="openLightbox(this)">
                    <img src="https://i.imgur.com/VusrOeO.jpeg" alt="Gallery 3" loading="lazy">
                    <div class="gallery-overlay"><span>Birthday Setup</span></div>
                </div>
                <div class="gallery-item reveal-scale" onclick="openLightbox(this)">
                    <img src="https://i.imgur.com/OvgoV26.jpeg" alt="Gallery 4" loading="lazy">
                    <div class="gallery-overlay"><span>Custom Creation</span></div>
                </div>
                <div class="gallery-item reveal-scale" onclick="openLightbox(this)">
                    <img src="https://i.imgur.com/ljOl2Bs.jpeg" alt="Gallery 5" loading="lazy">
                    <div class="gallery-overlay"><span>Chocolate Display</span></div>
                </div>
                <div class="gallery-item reveal-scale" onclick="openLightbox(this)">
                    <img src="https://i.imgur.com/zsyNbEJ.jpeg" alt="Gallery 6" loading="lazy">
                    <div class="gallery-overlay"><span>Glass Gifts</span></div>
                </div>
            </div>
        </div>
    </section>

    <!-- Lightbox -->
    <div class="lightbox" id="lightbox" onclick="closeLightbox()">
        <button class="lightbox-close" onclick="closeLightbox()">&times;</button>
        <img src="" alt="Gallery Preview" id="lightboxImg">
    </div>

    <!-- Why Choose Us -->
    <section class="section section-dark" id="why">
        <div class="container">
            <div class="section-header reveal">
                <span class="section-label">Why Choose Us</span>
                <h2 class="section-title">The Art of Occasions <em>Difference</em></h2>
                <p class="section-desc">We go beyond decoration — we create experiences that resonate with emotion and artistry.</p>
                <div class="divider-line"></div>
            </div>
            <div class="why-grid">
                <div class="why-card reveal">
                    <div class="why-icon">✦</div>
                    <h4>Fully Customized Creations</h4>
                    <p>Every design is uniquely tailored to your vision. No templates, no compromises — only bespoke creations that reflect your personal style.</p>
                </div>
                <div class="why-card reveal">
                    <div class="why-icon">◈</div>
                    <h4>Attention to Detail</h4>
                    <p>We obsess over every detail, from color palettes to textures, ensuring flawless execution that transforms spaces into experiences.</p>
                </div>
                <div class="why-card reveal">
                    <div class="why-icon">✧</div>
                    <h4>Unique Artistic Designs</h4>
                    <p>Our designs are one-of-a-kind masterpieces, crafted by creative minds who see beauty in the extraordinary and elegance in simplicity.</p>
                </div>
                <div class="why-card reveal">
                    <div class="why-icon">◇</div>
                    <h4>Premium Quality</h4>
                    <p>We source only the finest materials and decorations, ensuring every element meets the highest standards of luxury and durability.</p>
                </div>
                <div class="why-card reveal">
                    <div class="why-icon">❖</div>
                    <h4>Memorable Experiences</h4>
                    <p>Beyond aesthetics, we create moments that linger in memory. Every event becomes a story worth telling, a memory worth treasuring.</p>
                </div>
                <div class="why-card reveal">
                    <div class="why-icon">⬡</div>
                    <h4>Fast Communication</h4>
                    <p>Your time is precious. Our responsive team ensures seamless communication, quick turnarounds, and a smooth planning experience from start to finish.</p>
                </div>
            </div>
        </div>
    </section>

    <!-- Testimonials Section -->
    <section class="section section-light" id="testimonials">
        <div class="container">
            <div class="section-header reveal">
                <span class="section-label">Client Voices</span>
                <h2 class="section-title">What Our Clients <em>Say</em></h2>
                <p class="section-desc">The greatest reward is the joy reflected in our clients' words.</p>
                <div class="divider-line"></div>
            </div>
            <div class="testimonials-track" id="testimonialTrack">
                <div class="testimonial-card reveal">
                    <div class="testimonial-quote">"</div>
                    <div class="testimonial-stars">★ ★ ★ ★ ★</div>
                    <p class="testimonial-text">Art of Occasions transformed my daughter's birthday into a fairy tale. The attention to detail was extraordinary — every corner was a photo opportunity. Our guests were absolutely mesmerized.</p>
                    <div class="testimonial-author">
                        <div class="testimonial-avatar">N.K</div>
                        <div>
                            <div class="testimonial-name">Nadia Khalil</div>
                            <div class="testimonial-role">Birthday Celebration Client</div>
                        </div>
                    </div>
                </div>
                <div class="testimonial-card reveal">
                    <div class="testimonial-quote">"</div>
                    <div class="testimonial-stars">★ ★ ★ ★ ★</div>
                    <p class="testimonial-text">The chocolate arrangement they created for our wedding was a masterpiece. It wasn't just beautiful — it told our love story. The quality and presentation exceeded all expectations.</p>
                    <div class="testimonial-author">
                        <div class="testimonial-avatar">R.M</div>
                        <div>
                            <div class="testimonial-name">Rania Mansour</div>
                            <div class="testimonial-role">Chocolate Arrangement Client</div>
                        </div>
                    </div>
                </div>
                <div class="testimonial-card reveal">
                    <div class="testimonial-quote">"</div>
                    <div class="testimonial-stars">★ ★ ★ ★ ★</div>
                    <p class="testimonial-text">I ordered personalized glass gifts for my corporate event and was blown away by the craftsmanship. Every piece was perfect. Art of Occasions truly understands the meaning of premium quality.</p>
                    <div class="testimonial-author">
                        <div class="testimonial-avatar">S.H</div>
                        <div>
                            <div class="testimonial-name">Samer Haddad</div>
                            <div class="testimonial-role">Custom Glass Gifts Client</div>
                        </div>
                    </div>
                </div>
                <div class="testimonial-card reveal">
                    <div class="testimonial-quote">"</div>
                    <div class="testimonial-stars">★ ★ ★ ★ ★</div>
                    <p class="testimonial-text">From the first message to the final setup, the experience was flawless. They responded instantly, understood my vision perfectly, and delivered beyond what I imagined. Truly world-class service.</p>
                    <div class="testimonial-author">
                        <div class="testimonial-avatar">L.A</div>
                        <div>
                            <div class="testimonial-name">Lara Abdallah</div>
                            <div class="testimonial-role">Children's Party Client</div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- CTA Section -->
    <div class="cta-section">
        <div class="cta-bg-text">OCCASIONS</div>
        <div class="cta-content reveal">
            <span class="section-label">Ready to Begin?</span>
            <h2 class="cta-title">Let's Create Something <em>Extraordinary</em></h2>
            <p class="cta-desc">Your next celebration deserves the Art of Occasions touch. Reach out today and let us bring your vision to life.</p>
            <a href="https://wa.me/96181139230" target="_blank" class="btn-cta">
                <span>Start Your Journey</span>
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" width="18" height="18"><path d="M5 12h14M12 5l7 7-7 7"/></svg>
            </a>
        </div>
    </div>

    <!-- Footer -->
    <footer class="footer" id="contact">
        <div class="footer-grid">
            <div>
                <div class="footer-brand-logo">
                    <img src="https://i.imgur.com/WQHSNRf.jpeg" alt="Art of Occasions">
                    <span>Art of Occasions</span>
                </div>
                <p class="footer-brand-desc">Crafting extraordinary celebrations with exquisite artistry and meticulous attention to detail. Every occasion is an opportunity to create beauty.</p>
            </div>
            <div class="footer-col">
                <h5>Quick Links</h5>
                <ul>
                    <li><a href="#about">About Us</a></li>
                    <li><a href="#services">Services</a></li>
                    <li><a href="#gallery">Gallery</a></li>
                    <li><a href="#testimonials">Testimonials</a></li>
                </ul>
            </div>
            <div class="footer-col">
                <h5>Services</h5>
                <ul>
                    <li><a href="#services">Children's Parties</a></li>
                    <li><a href="#services">Birthday Celebrations</a></li>
                    <li><a href="#services">Chocolate Arrangements</a></li>
                    <li><a href="#services">Candle Gifts</a></li>
                    <li><a href="#services">Glass Gifts</a></li>
                </ul>
            </div>
            <div class="footer-col">
                <h5>Contact</h5>
                <div class="footer-contact-item">
                    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M22 16.92v3a2 2 0 01-2.18 2 19.79 19.79 0 01-8.63-3.07 19.5 19.5 0 01-6-6 19.79 19.79 0 01-3.07-8.67A2 2 0 014.11 2h3a2 2 0 012 1.72c.127.96.361 1.903.7 2.81a2 2 0 01-.45 2.11L8.09 9.91a16 16 0 006 6l1.27-1.27a2 2 0 012.11-.45c.907.339 1.85.573 2.81.7A2 2 0 0122 16.92z"/></svg>
                    <a href="tel:+96181139230" style="color: var(--gray-500); text-decoration: none;">81 139 230</a>
                </div>
                <div style="margin-top: 16px;">
                    <a href="https://wa.me/96181139230" target="_blank" class="btn-book" style="font-size: 0.7rem; padding: 10px 24px;">
                        <span>Get in Touch</span>
                        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" width="14" height="14"><path d="M5 12h14M12 5l7 7-7 7"/></svg>
                    </a>
                </div>
            </div>
        </div>
        <div class="footer-bottom">
            <span>&copy; 2025 Art of Occasions. All rights reserved.</span>
            <span>Crafted with passion & precision</span>
        </div>
    </footer>

    <!-- Back to Top -->
    <button class="back-to-top" id="backToTop" onclick="window.scrollTo({top:0,behavior:'smooth'})">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M12 19V5M5 12l7-7 7 7"/></svg>
    </button>

    <script>
    (function() {
        'use strict';

        // ============ PRELOADER ============
        window.addEventListener('load', function() {
            setTimeout(function() {
                document.getElementById('preloader').classList.add('hidden');
            }, 2200);
        });

        // ============ CUSTOM CURSOR ============
        const cursor = document.getElementById('cursor');
        let cursorX = 0, cursorY = 0, actualX = 0, actualY = 0;

        document.addEventListener('mousemove', function(e) {
            cursorX = e.clientX;
            cursorY = e.clientY;
        });

        function animateCursor() {
            actualX += (cursorX - actualX) * 0.12;
            actualY += (cursorY - actualY) * 0.12;
            cursor.style.left = actualX + 'px';
            cursor.style.top = actualY + 'px';
            requestAnimationFrame(animateCursor);
        }
        animateCursor();

        // Hover effect on interactive elements
        document.querySelectorAll('a, button, .gallery-item, .why-card, .testimonial-card').forEach(function(el) {
            el.addEventListener('mouseenter', function() { cursor.classList.add('hover'); });
            el.addEventListener('mouseleave', function() { cursor.classList.remove('hover'); });
        });

        // ============ NAVBAR ============
        const navbar = document.getElementById('navbar');
        let lastScroll = 0;

        window.addEventListener('scroll', function() {
            const scrollY = window.pageYOffset;
            if (scrollY > 80) {
                navbar.classList.add('scrolled');
            } else {
                navbar.classList.remove('scrolled');
            }
            lastScroll = scrollY;

            // Back to top
            const btt = document.getElementById('backToTop');
            if (scrollY > 600) {
                btt.classList.add('visible');
            } else {
                btt.classList.remove('visible');
            }
        });

        // ============ MOBILE MENU ============
        const menuToggle = document.getElementById('menuToggle');
        const mobileMenu = document.getElementById('mobileMenu');

        menuToggle.addEventListener('click', function() {
            menuToggle.classList.toggle('active');
            mobileMenu.classList.toggle('open');
            document.body.style.overflow = mobileMenu.classList.contains('open') ? 'hidden' : '';
        });

        mobileMenu.querySelectorAll('a').forEach(function(link) {
            link.addEventListener('click', function() {
                menuToggle.classList.remove('active');
                mobileMenu.classList.remove('open');
                document.body.style.overflow = '';
            });
        });

        // ============ SCROLL REVEAL ============
        function revealElements() {
            const reveals = document.querySelectorAll('.reveal, .reveal-left, .reveal-right, .reveal-scale');
            const windowHeight = window.innerHeight;

            reveals.forEach(function(el, i) {
                const elementTop = el.getBoundingClientRect().top;
                const revealPoint = 120;

                if (elementTop < windowHeight - revealPoint) {
                    el.style.transitionDelay = (i % 4) * 0.08 + 's';
                    el.classList.add('active');
                }
            });
        }

        window.addEventListener('scroll', revealElements);
        window.addEventListener('load', function() {
            setTimeout(revealElements, 300);
        });

        // ============ PARALLAX ============
        window.addEventListener('scroll', function() {
            const parallaxBgs = document.querySelectorAll('[data-parallax-bg]');
            parallaxBgs.forEach(function(bg) {
                const section = bg.closest('.parallax-section');
                if (!section) return;
                const rect = section.getBoundingClientRect();
                const speed = 0.3;
                const yPos = rect.top * speed;
                bg.style.transform = 'translateY(' + yPos + 'px)';
            });
        });

        // ============ SMOOTH ANCHOR SCROLL ============
        document.querySelectorAll('a[href^="#"]').forEach(function(anchor) {
            anchor.addEventListener('click', function(e) {
                const target = document.querySelector(this.getAttribute('href'));
                if (target) {
                    e.preventDefault();
                    const offset = 80;
                    const y = target.getBoundingClientRect().top + window.pageYOffset - offset;
                    window.scrollTo({ top: y, behavior: 'smooth' });
                }
            });
        });

        // ============ TESTIMONIALS HORIZONTAL SCROLL ============
        const track = document.getElementById('testimonialTrack');
        let isDown = false, startX, scrollLeft;

        if (track) {
            track.addEventListener('mousedown', function(e) {
                isDown = true;
                startX = e.pageX - track.offsetLeft;
                scrollLeft = track.scrollLeft;
                track.style.cursor = 'grabbing';
            });
            track.addEventListener('mouseleave', function() {
                isDown = false;
                track.style.cursor = 'grab';
            });
            track.addEventListener('mouseup', function() {
                isDown = false;
                track.style.cursor = 'grab';
            });
            track.addEventListener('mousemove', function(e) {
                if (!isDown) return;
                e.preventDefault();
                const x = e.pageX - track.offsetLeft;
                const walk = (x - startX) * 1.5;
                track.scrollLeft = scrollLeft - walk;
            });
            track.style.cursor = 'grab';
            track.style.overflowX = 'auto';
            track.style.scrollbarWidth = 'none';
            track.style.msOverflowStyle = 'none';

            // Touch support
            track.addEventListener('touchstart', function(e) {
                startX = e.touches[0].pageX - track.offsetLeft;
                scrollLeft = track.scrollLeft;
            });
            track.addEventListener('touchmove', function(e) {
                const x = e.touches[0].pageX - track.offsetLeft;
                const walk = (x - startX) * 1.5;
                track.scrollLeft = scrollLeft - walk;
            });
        }

        // ============ COUNTER ANIMATION ============
        function animateCounters() {
            document.querySelectorAll('.intro-stat-num').forEach(function(counter) {
                if (counter.dataset.animated) return;
                const rect = counter.getBoundingClientRect();
                if (rect.top > window.innerHeight) return;
                counter.dataset.animated = 'true';

                const text = counter.textContent;
                const num = parseInt(text);
                const suffix = text.replace(/[0-9]/g, '');
                const duration = 2000;
                const start = performance.now();

                function step(now) {
                    const progress = Math.min((now - start) / duration, 1);
                    const eased = 1 - Math.pow(1 - progress, 3);
                    const current = Math.round(num * eased);
                    counter.textContent = current + suffix;
                    if (progress < 1) requestAnimationFrame(step);
                }
                requestAnimationFrame(step);
            });
        }
        window.addEventListener('scroll', animateCounters);

        // ============ TILT EFFECT ON SERVICE IMAGES ============
        document.querySelectorAll('.service-image-col').forEach(function(card) {
            card.addEventListener('mousemove', function(e) {
                const rect = card.getBoundingClientRect();
                const x = e.clientX - rect.left;
                const y = e.clientY - rect.top;
                const centerX = rect.width / 2;
                const centerY = rect.height / 2;
                const rotateX = (y - centerY) / 30;
                const rotateY = (centerX - x) / 30;
                card.style.transform = 'perspective(1000px) rotateX(' + rotateX + 'deg) rotateY(' + rotateY + 'deg)';
            });
            card.addEventListener('mouseleave', function() {
                card.style.transform = 'perspective(1000px) rotateX(0) rotateY(0)';
                card.style.transition = 'transform 0.5s ease';
            });
            card.addEventListener('mouseenter', function() {
                card.style.transition = 'none';
            });
        });

    })();

    // ============ LIGHTBOX ============
    function openLightbox(el) {
        const img = el.querySelector('img');
        const lightbox = document.getElementById('lightbox');
        const lightboxImg = document.getElementById('lightboxImg');
        lightboxImg.src = img.src;
        lightbox.classList.add('open');
        document.body.style.overflow = 'hidden';
    }
    function closeLightbox() {
        document.getElementById('lightbox').classList.remove('open');
        document.body.style.overflow = '';
    }
    document.addEventListener('keydown', function(e) {
        if (e.key === 'Escape') closeLightbox();
    });
    </script>
</body>
</html>
